<aside class="sidebar" id="sidebar">

    <button id="sidebar-toggle" class="sidebar-handle">
        <i class="fa-solid fa-chevron-left"></i>
    </button>

    <nav
        class="sidenav sidenav-filters"
        aria-label="Navigation latérale"
    >

        {% if app.user and (is_granted('ROLE_USER') or is_granted('ROLE_CUSTOMER')) %}

            <ul class="items-list standalone-links">

                <li>
                    <a
                        href="{{ path('dossier_user_list') }}"
                        data-tooltip="Mes dossiers"
                    >
                        <i class="fa-solid fa-folder-open"></i>
                        <span>Mes dossiers</span>
                    </a>
                </li>

            </ul>

        {% endif %}

        {# VEHICULES #}
        <details class="sidebar-group">
            <summary data-tooltip="Véhicules">
                <i class="fa-solid fa-car-side"></i>
                <span>Véhicules</span>
            </summary>
            <ul class="items-list">

                <li>
                    <a
                        href="{{ path('vehicles_index') }}"
                        data-tooltip="Catalogue véhicules"
                    >
                        <i class="fa-solid fa-car-tunnel"></i>
                        <span>Catalogue véhicules</span>
                    </a>
                </li>

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}

                    <li>
                        <a
                            href="{{ path('vehicles_models') }}"
                            data-tooltip="Catalogue fabricants"
                        >
                            <i class="fa-solid fa-book"></i>
                            <span>Catalogue fabricants</span>
                        </a>
                    </li>

                    <li>
                        <a
                            href="{{ path('vehicle_new') }}"
                            data-ajax-modal
                            data-tooltip="Créer un véhicule"
                        >
                            <i class="fa-solid fa-file-circle-plus"></i>
                            <span>Créer un véhicule</span>
                        </a>
                    </li>

                    <li>
                        <a
                            href="{{ path('vehicle_model_new') }}"
                            data-ajax-modal
                            data-tooltip="Créer un modèle de véhicule"
                        >
                            <i class="fa-solid fa-file-circle-plus"></i>
                            <span>Créer un modèle de véhicule</span>
                        </a>
                    </li>

                {% endif %}

            </ul>

        </details>

        {# CLIENTS #}

        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}

            <details class="sidebar-group">

                <summary data-tooltip="Clients">

                    <i class="fa-regular fa-address-book"></i>

                    <span>Clients</span>

                </summary>

                <ul class="items-list">

                    <li>
                        <a
                            href="{{ path('customer_list') }}"
                            data-tooltip="Liste clients"
                        >
                            <i class="fa-regular fa-address-book"></i>
                            <span>Liste clients</span>
                        </a>
                    </li>

                    <li>
                        <a
                            href="{{ path('customer_new') }}"
                            data-ajax-modal
                            data-tooltip="Créer client"
                        >
                            <i class="fa-solid fa-user-plus"></i>
                            <span>Créer client</span>
                        </a>
                    </li>

                </ul>

            </details>

        {% endif %}

        {# DOSSIERS #}

        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}

            <details class="sidebar-group">

                <summary data-tooltip="Dossiers">

                    <i class="fa-solid fa-folder-open"></i>

                    <span>Dossiers</span>

                </summary>

                <ul class="items-list">

                    <li>
                        <a
                            href="{{ path('admin_dossier_list') }}"
                            data-tooltip="Liste dossiers (Admin)"
                        >
                            <i class="fa-solid fa-list"></i>
                            <span>Liste dossiers (Admin)</span>
                        </a>
                    </li>

                    <li>
                        <a
                            href="{{ path('admin_dossier_new') }}"
                            data-tooltip="Créer dossier"
                        >
                            <i class="fa-solid fa-file-circle-plus"></i>
                            <span>Créer dossier</span>
                        </a>
                    </li>

                </ul>

            </details>

        {% endif %}

    </nav>

</aside>