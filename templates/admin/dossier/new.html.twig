{% extends 'base.html.twig' %}

{% block body %}
    <div class="container form-ui">

        <h1 class="text-center">{{ title }}</h1>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}

        {% include 'admin/dossier/form.html.twig' %}

    </div>
{% endblock %}