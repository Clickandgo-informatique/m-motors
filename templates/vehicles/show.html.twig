{% extends "base.html.twig" %}
{% block title %}{{title}}{% endblock %}
{% block body %}
<a href="{{path("vehicles_index")}}"class="btn btn-primary"><i class="fa-solid fa-list"></i>Revenir à la liste</a>
<h1 class="text-center">{{title}}</h1>
<h3 class="text-center">{{vehicle.vehicleModel.brand.name}}&nbsp;{{vehicle.vehicleModel.model.name}}</h3>

{% endblock %}

