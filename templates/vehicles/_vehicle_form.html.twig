{% block body %}

	{{ form_start(form) }}

	<div class="page-container vehicle-edit">
		<h1 class="text-center">
			<i class="fa-solid fa-pen-to-square"></i>
			Modifier une fiche véhicule
		</h1>

		<div
			class="vehicle-layout">

			{# Colonne gauche : formulaire #}
			<div class="vehicle-left">

				<div class="card vehicle-form-card">

					<div class="card-body">

						<div class="card-header">
							{% if vehicle.vehicleModel and vehicle.vehicleModel.brand and vehicle.vehicleModel.model %}

								<h1 class="form-row">
									{{ vehicle.vehicleModel.brand.name }}
								</h1>

								<h2 class="form-row">
									{{ vehicle.vehicleModel.model.name }}
								</h2>

							{% endif %}
						</div>

						{# Usage type #}
						<div class="form-row">
							<div class="usage-type-selector">

								{{ form_label(form.usageType) }}

								<div class="usage-type-options">
									{% for option in form.usageType %}
										<label class="usage-card usage-confirm">
											{{ form_widget(option) }}

											<span class="usage-card-content">
												{{ option.vars.label|raw }}
											</span>
										</label>
									{% endfor %}
								</div>

								{{ form_errors(form.usageType) }}

							</div>
						</div>

						<details open>
							<summary>Données techniques</summary>

							<div class="form-row">
								<div class="form-group">
									{{ form_label(form.status) }}
									{{ form_widget(form.status) }}
									{{ form_errors(form.status) }}
								</div>

								<div class="form-group">
									{{ form_label(form.supplier) }}
									{{ form_widget(form.supplier) }}
									{{ form_errors(form.supplier) }}
								</div>
							</div>

							<div class="form-row">
								<div class="form-group autocomplete-container flex-3">

									{{ form_label(form.vehicleModelSearch) }}

									<div class="dropdown-wrapper">

										{{ form_widget(form.vehicleModelSearch, {
                                        attr: {
                                            autocomplete: 'off',
                                            'data-autocomplete': 'true',
                                            'data-result-div': '#vehicle-model-results',
                                            'data-hidden-target': '#vehicle_vehicleModel',
											'link-mode':'1'
                                        }
                                    }) }}

										{{ form_widget(form.vehicleModel, {
                                        attr: { style: 'display:none;' }
                                    }) }}

										<div id="vehicle-model-results" class="dropdown-results"></div>

									</div>

									{{ form_errors(form.vehicleModelSearch) }}

								</div>

								<div class="form-actions flex-1">
									<a href="javascript:void(0)">
										<i class="fa-regular fa-folder-open"></i>
									</a>

									<a href="{{ path('vehicle_model_new') }}" data-ajax-modal>
										<i class="fa-solid fa-circle-plus"></i>
									</a>
								</div>
							</div>

							<div class="form-row">
								<div class="form-group">
									{{ form_label(form.vin) }}
									{{ form_widget(form.vin) }}
									{{ form_errors(form.vin) }}
								</div>

								<div class="form-group">
									{{ form_label(form.registrationNumber) }}
									{{ form_widget(form.registrationNumber) }}
									{{ form_errors(form.registrationNumber) }}
								</div>
							</div>

							<div class="form-row">
								<div class="form-group">
									{{ form_label(form.mileage) }}
									{{ form_widget(form.mileage) }}
									{{ form_errors(form.mileage) }}
								</div>

								<div class="form-group">
									{{ form_label(form.firstRegistrationDate) }}
									{{ form_widget(form.firstRegistrationDate) }}
									{{ form_errors(form.firstRegistrationDate) }}
								</div>

								<div class="form-group">
									{{ form_label(form.price) }}
									{{ form_widget(form.price) }}
									{{ form_errors(form.price) }}
								</div>
							</div>

							<div class="form-row">
								<div class="form-group">
									{{ form_label(form.color) }}
									{{ form_widget(form.color) }}
									{{ form_errors(form.color) }}
								</div>

								<div class="form-group">
									{{ form_label(form.fuelType) }}
									{{ form_widget(form.fuelType) }}
									{{ form_errors(form.fuelType) }}
								</div>

								<div class="form-group">
									{{ form_label(form.gearType) }}
									{{ form_widget(form.gearType) }}
									{{ form_errors(form.gearType) }}
								</div>
							</div>

						</details>

						<details open>
							<summary>Options</summary>

							<div class="vehicle-features-list">
								{% if vehicle.vehicleModel.features|length > 0 %}
									{% for feature in vehicle.vehicleModel.features|sort((a, b) => a.label <=> b.label) %}
										{{ feature.label }}
										{% if not loop.last %},
										{% endif %}
									{% endfor %}
								{% else %}
									<p class="alert alert-warning text-center">
										Aucune option enregistrée
									</p>
								{% endif %}
							</div>

						</details>

					</div>
				</div>

			</div>

			{# Colonne droite : images #}
			<div class="vehicle-right">

				<div class="card vehicle-gallery-card">

					<div class="card-header">
						Galerie
					</div>

					<div class="card-body">

						{% set isNew = vehicle.id is null %}

						<div class="dropzone" data-mode="media" data-upload-url="{{ path('vehicle_image_upload', {id: vehicle.id}) }}" data-delete-url="{{ path('vehicle_image_delete', {id: '__id__'}) }}" data-documents-url="{{ isNew ? '' : path('vehicle_images', {id: vehicle.id}) }}" data-preview-id="vehicle-gallery-preview" data-destination="{{ vehicle.id ?? '' }}" data-status="{{ vehicle.status?.value ?? 'draft' }}" data-locked="{{ isNew ? 1 : 0 }}">

							<p>Glissez vos fichiers ici ou cliquez pour ajouter</p>

							<button type="button" class="dz-upload-btn" disabled>
								Envoyer les fichiers
							</button>

						</div>

						<div id="vehicle-gallery-preview"></div>

						{% include "vehicles/_vehicle_images_gallery.html.twig" %}

					</div>
				</div>

			</div>

		</div>

		<div class="form-actions">
			<button type="submit" class="btn btn-primary">
				<i class="fa-solid fa-floppy-disk"></i>
				Enregistrer
			</button>
		</div>

	</div>

	{{ form_end(form) }}

{% endblock %}
