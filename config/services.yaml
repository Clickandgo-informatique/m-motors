parameters:
    # Dossier racine des uploads publics
    upload_dir: "%kernel.project_dir%/public/uploads"

    # Uploads dossiers
    dossier_upload_dir: "%upload_dir%/dossiers"

    # Uploads véhicules
    vehicle_upload_dir: "%upload_dir%/vehicles"

services:
    _defaults:
        autowire: true
        autoconfigure: true

    # =========================================================
    # AUTOWIRE GLOBAL APP
    # =========================================================
    App\:
        resource: "../src/"
        exclude:
            - "../src/DependencyInjection/"
            - "../src/Entity/"
            - "../src/Migrations/"
            - "../src/Tests/"
            - "../src/Kernel.php"

    # =========================================================
    # IMAGE PROCESSOR
    # =========================================================
    App\Service\ImageProcessor:
        bind:
            string $uploadDir: "%upload_dir%"

    # =========================================================
    # DOSSIERS
    # =========================================================
    App\Service\DossierDocumentUploadService:
        bind:
            string $uploadDir: "%dossier_upload_dir%"

    App\Service\DossierDocumentTypeResolver: ~

    # =========================================================
    # VEHICLES
    # =========================================================
    App\Service\VehicleGalleryManager: ~

    # =========================================================
    # WORKFLOW GUARD
    # =========================================================
    App\Workflow\DossierWorkflowGuard:
        tags:
            - {
                  name: kernel.event_listener,
                  event: workflow.dossier.guard.submit_documents,
                  method: guardSubmitDocuments,
              }

    # IMPORTANT :
    # Aucun service pour DocumentUploadedListener ici
    # car on utilise un EventSubscriber autoconfiguré
